# dev-diary

Working documents for Dry Run. None of this ships to the browser.

| File | What it is |
|---|---|
| `project.md` | The product specification. What Dry Run is and why. |
| `task.md` | The build breakdown. Atomic tasks, dependencies, and what can run in parallel. |

The visual design reference lives outside the repo for now, as a published
artifact. It supersedes the Stitch specification wherever the two conflict.
